// lib/settings.ts

/**
 * In-memory barber settings.
 * Replace with a real store (DB) later.
 */
export const barberSettings = {
    startHour: 9,       // opens at 09:00
    endHour: 17,        // closes at 17:00
    slotDuration: 60,   // minutes per slot
  };
  